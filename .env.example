# Project Configuration
# Change PROJECT_NAME when running multiple instances
PROJECT_NAME=starter-kit

# Port Configuration
# Change these ports if they conflict with other services
DB_PORT=5432
BACKEND_PORT=8080
FRONTEND_PORT=3000

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=starter_kit
